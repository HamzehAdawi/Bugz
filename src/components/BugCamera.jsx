import react, { useEffect } from 'react';

const BugCamera = () => {

    useEffect(() => {
        const dirtCanvas = document.getElementById('dirt-canvas');
        const boundaries = dirtCanvas.getBoundingClientRect();

        


    }, []);
}

export default BugCamera;